# Arch packaging (AUR) — to follow

A `PKGBUILD` that builds the pinned Remmina base + the AVD/PIV plugin (`provides`/
`conflicts` the stock `remmina`/RDP plugin), against Arch's FreeRDP 3. Stock Arch
Remmina lacks the AAD web-auth base, so the base is built here too.
